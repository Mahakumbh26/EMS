[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn ems.asgi:application -k uvicorn.workers.UvicornWorker --workers ${WEB_CONCURRENCY:-1} --bind 0.0.0.0:$PORT"
healthcheckPath = "/metrics"
healthcheckTimeout = 10
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
